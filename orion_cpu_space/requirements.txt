# ================================================
# ORION Neural Hub — CPU Free Tier
# Lightweight: fits in 2 vCPU + 16GB RAM
# NO piper-tts (requires libespeak-ng system dep)
# ================================================

# Core
gradio==5.33.0
fastapi
uvicorn
python-multipart

# Testing
pytest

# PDF Processing
pymupdf==1.25.3

# OCR (CPU mode)
easyocr>=1.7.2
Pillow>=10.4.0

# Embeddings (CPU — small model)
sentence-transformers>=3.3.0

# Vision (CPU — lightweight models)
transformers>=4.40.0
torch>=2.1.0

# Utilities
numpy>=1.26.0
huggingface_hub>=0.26.0
