# ================================================
# ORION Neural Hub — ZeroGPU
# ================================================

# Core
gradio==5.33.0

# PDF Processing
pymupdf==1.25.3

# TTS (edge-tts — Microsoft Edge free TTS, no API key needed)
edge-tts>=6.1.0

# ONNX Runtime (for vision/other models)
onnxruntime==1.20.1

# OCR (CPU mode)
easyocr>=1.7.2
Pillow>=10.4.0

# Embeddings (CPU — small model)
sentence-transformers>=3.3.0

# Phi-3 Vision: uses eager attention (no flash_attn needed)

# Utilities
numpy>=1.26.0
huggingface_hub>=0.26.0
