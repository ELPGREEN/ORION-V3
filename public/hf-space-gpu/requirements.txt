# ================================================
# ORION Neural Hub — CPU Free Tier
# Lightweight: fits in 2 vCPU + 16GB RAM
# ================================================

# Core
gradio==5.33.0

# PDF Processing
pymupdf==1.25.3

# TTS (Piper ONNX — CPU only, lightweight)
# piper-tts removed: piper-phonemize incompatible with Python 3.13
# Using piper-tts via pre-built wheel or direct ONNX inference
onnxruntime==1.20.1
piper-phonemize==1.1.0; python_version < "3.13"

# OCR (CPU mode)
easyocr>=1.7.2
Pillow>=10.4.0

# Embeddings (CPU — small model)
sentence-transformers>=3.3.0

# Utilities
numpy>=1.26.0
huggingface_hub>=0.26.0
