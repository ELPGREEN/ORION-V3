# ================================================
# ORION Neural Hub — ZeroGPU v3.0
# ================================================

# Core
gradio==5.33.0

# PDF Processing
pymupdf==1.25.3

# TTS (edge-tts — Microsoft Edge free TTS, no API key needed)
edge-tts>=6.1.0

# Audio processing
pydub>=0.25.1

# ONNX Runtime (for vision/other models)
onnxruntime==1.20.1

# OCR (CPU mode)
easyocr>=1.7.2
Pillow>=10.4.0

# Embeddings (CPU — small model)
sentence-transformers>=3.3.0

# CNN models (DETR, SegFormer, DPT, ResNet)
torchvision>=0.17.0

# Utilities
numpy>=1.26.0
huggingface_hub>=0.26.0
