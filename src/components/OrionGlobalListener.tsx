/**
 * OrionGlobalListener — DISABLED.
 * Voice recognition is now exclusively handled by GlobalOrionListener (dashboard).
 * This component is kept as a no-op to avoid breaking imports.
 */
export function OrionGlobalListener() {
  return null;
}
